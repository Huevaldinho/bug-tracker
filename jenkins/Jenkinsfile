pipeline {
    agent any

    stages {
        stage('Unit Tests - Back End') {
            agent {
                docker {
                    image 'golang:tip-trixie'
                    reuseNode true
                }
            }
            steps {
                 dir('bug-tracker-backend') {
                    sh 'go test -v ./...'
                }
            }
        }
    }
}
