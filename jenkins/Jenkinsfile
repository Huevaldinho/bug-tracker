pipeline {
    agent any

    stages {
        stage('Unit Tests - Back End') {
            agent {
                docker {
                    image 'tip-trixie',
                    reuseNode true
                }
            }
            steps {
                 dir('bug-tracker-backend') {
                    sh 'go test -v ./...'
                }
            }
        }
    }
}
