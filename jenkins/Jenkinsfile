pipeline {
    agent any

    stages {
        stage('Unit Tests - Back End') {
            steps {
                 dir('bug-tracker-backend') {
                    sh 'go test -v ./...'
                }
            }
        }
    }
}
